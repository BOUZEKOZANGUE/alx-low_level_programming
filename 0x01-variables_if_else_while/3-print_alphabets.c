#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z');
		putchar(letter);
	for (letter = 'A'; letter <= 'Z');
		putchar(letter);

	putchar('\n');

	return (0);
}
